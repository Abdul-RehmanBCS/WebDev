<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>TechMart</title>
    <link rel="stylesheet" href="/css/style.css">
    <% if (typeof bootstrap !== 'undefined' && bootstrap) { %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <% } %>
    <% if (typeof additionalCSS !== 'undefined') { %>
    <link rel="stylesheet" href="<%= additionalCSS %>">
    <% } %>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <nav class="nav">
            <div class="container">
                <div class="nav-wrapper">
                    <div class="logo">
                        <a href="/">TechMart</a>
                    </div>
                    <ul class="nav-menu" id="navMenu">
                        <li><a href="/">Home</a></li>
                        <li><a href="/products">Products</a></li>
                        <li><a href="/#features">Features</a></li>
                        <li><a href="/#testimonials">Reviews</a></li>
                        <li><a href="/cart" class="cart-link">
                            Cart <span class="cart-badge" id="cartBadge"><%= typeof cartCount !== 'undefined' ? cartCount : 0 %></span>
                        </a></li>
                        <li><a href="/checkout">Checkout</a></li>
                        <% if (typeof admin !== 'undefined' && admin) { %>
                        <li><a href="/admin">Admin</a></li>
                        <% } %>
                    </ul>
                    <button class="nav-toggle" id="navToggle" aria-label="Toggle navigation">
                        <span></span>
                        <span></span>
                        <span></span>
                    </button>
                </div>
            </div>
        </nav>
    </header>

